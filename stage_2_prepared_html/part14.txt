Transform the input text into UI-oriented HTML.

Rules:
1. Every piece of text must be wrapped in an HTML element.
2. Use classic HTML tags (section, header, h1–h6, p, ul, ol, li, pre, code, div).
3. Add a data-ui attribute describing the UI role of each element.
   Examples: block, section-header, label, heading, paragraph, bullet-list, numbered-list, list-item, code-block, qa-block, question, answer, tasks, task, metric-list, metric.
4. Preserve all original text. Do not remove or summarize anything.
5. Detect headings and map them to h2 with data-ui="heading" (or other levels if obvious).
6. For algorithm or code-like content, use:
   <pre data-ui="code-block" data-language="algorithm"><code>…</code></pre>
7. Do not output Markdown. Output only HTML.
8. Do not wrap the entire output in backticks or fences.

Output: pure HTML with data-ui attributes.
Input:
<blok-14>
Název kroku: Motion & Micro-animations  
Hlavní záměr: Přidat vizuálům kinetickou sílu a zvýšit engagement.

Obsah kroku:

* Motion není dekorace — je to řízení pozornosti v čase. Kde typografie a barva modelují statickou hierarchii, tam motion vytváří sekvenční prioritu: co uživatel uvidí jako první, co jako druhé a kde má vizuál vyvolat napětí. Základní animační principy (anticipation, squash & stretch, timing, easing, follow-through) přenesené do marketingového prostředí zvyšují scroll-stop rate, watch-time i retenci.
* Příklady:
  • Anticipation: nepatrné vychýlení CTA před animací (např. 2 px dolů) připraví oko na očekávaný pohyb.
  • Timing: rychlost animace signálu (0.15–0.3 s) přímo ovlivňuje energii. Rychlý timing → výkon; pomalý timing → elegance.
  • Easing: ease-out u CTA znamená přirozené „doznění“, méně agresivní než linear.
  • Squash & stretch: u ikon funguje subtilně — opticky zvýší živost a lidskost vizuálu.
* Případová studie:
  Landing page měla nízkou konverzi z videa. Po implementaci micro-animace headline (jemné fade-in + upward motion o 8 px), CTA bounce s minimální amplitudou (cca 3 px), a přechodu barvy pozadí z 900 → 800 došlo ke zvýšení watch-time o 17 % a kliknutí na CTA o 12 %.
* Typické pohovorové otázky a optimální odpovědi:
  • „Jak poznáte, že animace není rušivá?“ → „Animace musí podpořit sdělení a mít jasnou funkci. Kontroluji, zda nenarušuje čtecí rytmus a zda trvá maximálně 0.3–0.6 s.“
  • „Jak pracujete s motion timingem napříč platformami?“ → „Na TikToku volím agresivní timing (0.1–0.2 s), na YouTube 0.3–0.5 s a u bannerů subtilní animace pod 0.2 s.“
  • „Proč micro-animace zvyšují engagement?“ → „Oko reaguje na predikční chyby. Malý pohyb vytvoří novou informaci, kterou mozek musí vyřešit — tím získá vizuál další pozornost.“
* Mini úkoly:
  • Vytvoř 3 micro-animace pro jednu kampaň: animace claimu (fade-in + slight scale), animace CTA (easing-out pulse), animace ikony (micro-jump).
  • Otestuj, zda animace nezpožďuje čitelnost nebo nerozbíjí barevnou hierarchii z kroku 13.
* Návaznost na krok 13: Barva a typografie definují statickou logiku sdělení. Motion ji rozšiřuje o časovou dramaturgii — přidáváš další rozměr řízení pozornosti.

Praktická část:

```
# Jednoduchý keyframe engine pro micro-animace (pseudokód)
element = "cta_button"

animation = {
    "frames": [
        {"t": 0.0, "scale": 1.00, "y": 0},
        {"t": 0.15, "scale": 1.04, "y": -3},
        {"t": 0.30, "scale": 1.00, "y": 0}
    ],
    "easing": "ease-out"
}

def play(anim):
    for f in anim["frames"]:
        print(f)
        
play(animation)
```

Strukturovaný problém:

1. Vyber claim.
2. Navrhni pro něj animaci: vstup (anticipation), hlavní pohyb (message reveal), doznění (ease-out).
3. Ověř, zda animace posiluje nebo ruší hierarchii vizuálu.
4. Vyhodnoť změnu „čas do pochopení“ před a po animaci.

Ukazatele úspěchu:

* Micro-animace podporuje sdělení, neodvádí pozornost.
* Animace má jasné fáze (anticipation → action → settle).
* Engagement se měřitelně zvyšuje (scroll-stop rate, watch-time, CTR).
* Umíš u pohovoru popsat, proč konkrétní animace funguje z hlediska psychologie pozornosti.

<inner-block-14>
Bonus / laterální rozšíření:
- Rekonstruuj jednu známou reklamu v minimalistickém motion stylu. Použij pouze 3 objekty: text, barvu a jeden abstraktní tvar. Přenes principy filmové animace (motion arcs, timing curves, rytmus střihu) do krátkého marketingového mikrosystému. Tento trénink rozvíjí schopnost redukovat komplexní vizuální jazyk na čisté, účinné pohybové signály.  
<end-inner-block-14>
<end-blok-14>
